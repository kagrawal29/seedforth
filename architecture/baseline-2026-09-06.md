# SeedForth Baseline Inventory — 2026-09-06

**Status:** Historical observational baseline; use the reconciliation ledger for current state
**Collection:** local workspace, GitHub metadata, new server, and live Neo4j

## Local workspace

The SeedForth root is a Git repository containing registry material and many independent Git repositories. It is not a monorepo today. The local workspace is dirty and contains untracked experiments and runtime-adjacent artifacts.

Important local repositories include:

| Local path | Current local state |
|---|---|
| `tetrahedron/projects/mycelium` | feature branch; handoff committed; substantial uncommitted architecture/graph work |
| `delta` | `main`; tracked commit aligned with its local upstream; local kanban/session changes |
| `tetrahedron` | `main`; local documentation and artifact changes |
| `flowing-indian` | feature branch; local code/test/config changes |
| `seedforthing` | `master`; clean at collection time |
| `website` | `main`; clean at collection time |

## GitHub observations

- `kagrawal29/mycelium` has a newer `main` branch than the local Mycelium feature branch.
- `kagrawal29/delta:main` is at the same platform code commit used by the new server.
- `kagrawal29/tetrahedron:main` differs from the local checkout.
- `kagrawal29/seedforth-website:main` matches the local website checkout.
- `Seedforth/seedforthing:master` differs from the local checkout.
- Flowing Indian uses a separate repository owner and requires separate GitHub access verification.

These are observations, not permission to merge, reset, delete, or force-sync any checkout.

## New server observations

- Delta service: active and enabled.
- Neo4j Mycelium: active.
- WAHA: active.
- Eight supervised opencode agents: running.
- Delta registry: seven active project entries, plus the separately managed hub.
- Server Delta checkout: clean tracked files with expected runtime-generated untracked state.
- Per-agent checkouts: present and contain expected runtime-modified transport files/logs.

## Live graph observations

- 30,383 nodes.
- 25,544 relationships.
- 33 enabled protocols across heartbeat, dream, deep, fast, session-start, and on-demand cadences.
- 3 active Flowing Indian division subagents.
- 3 Flowing Indian workstreams: one active and two planned.
- 3 pending Flowing Indian human decisions.
- 8 active runtime project agents.
- Latest observed ProtocolRun: 2026-09-06 09:30 UTC.

## Historical conflicts and current disposition

1. Flowing Indian’s graph/runtime status required reconciliation; current fleet
   mapping is maintained by the deployed seeder and heartbeat.
2. Local Mycelium documentation mixed current SeedForth architecture with
   inherited Maverick/team-distribution language; the platform copy now has an
   explicit legacy boundary.
3. Mycelium and Delta remain separate upstream repositories but are consolidated
   into the deployed SeedForth platform release.
4. Runtime status now has a read-only reconciliation report spanning supervisor,
   Delta registry, Git, filesystem, and Neo4j.
5. Server Git credential handling was moved to the external environment
   contract; product token history remains an explicitly documented exception.

## Historical next actions (completed or superseded)

1. The machine-readable registry and read-only reconciler now provide the
   current repository/remote/server snapshot.
2. GitHub branch SHAs are checked through remote inspection and recorded in the
   reconciliation ledger.
3. Platform server credentials are externalized; the Seedforthing product token
   remains a documented remediation item.
4. The runtime reconciler and deployed fleet graph model are operational.
5. Remaining work is product-repository merge/cleanup decisions, not platform
   cutover.
