FROM python:3.12-slim

WORKDIR /app

# Copy the embed-proxy script from the deploy directory
COPY embed-proxy.py .

# No external dependencies — uses Python stdlib only
RUN chmod +x embed-proxy.py

EXPOSE 7701

# Run the proxy
CMD ["python3", "embed-proxy.py"]
