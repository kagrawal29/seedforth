## Team demand signals

- Can Claude autonomously poll another Claude Code session's JSONL conversation file every 60-120 seconds and report progress without user input?
- Should superpowers skills be embedded into the spec-to-ship playbook phases, or left as standalone invocations? Which approach preserves quality without breaking the workflow?
- Does Claude automatically select and invoke the right superpowers skill, or does the user have to explicitly trigger it each time?
- How can I disable CI tests on GitHub without touching local hooks, and re-enable when needed?
- Should we run an expert panel to validate our tech stack decisions, and how do we scope it to tooling only (not processes)?
- When 10 specs exist for a single product, how do they reference shared concepts without cross-contaminating or overwriting each other?
- Does Nango provide pre-built connectors for every API including contacts, lead data from Salesforce and HubSpot, or just standard CRUD endpoints?
- How do we implement multi-fund hierarchy in the frontend with fund switching, spec-to-plan accuracy checking, and proper branch strategy?
- How do we make the intelligence pipeline itself a graph structure — PipelineStage nodes holding Cypher queries as properties, with FLOWS_TO edges defining execution order?
- How do we verify the meta-intelligence system is fully functional before distributing to the team?
- How should GitHub issues be used as a working scratchpad during meta-system development sessions?
- What are L1-L8 in the meta-intelligence system and which layers are built vs not built yet?
- What is the correct OR/AND group logic for rule conditions, especially for equals vs not_equals conditions across single-value fields?
- How can I test all rules against real data in a way where Claude is not forcing them to pass, but actually analyzing the whole system?
- Why do system categories appear in the org category dropdown when creating rules — only organization categories should show.
- What is Asgard graph and how does it serve the team?
- How do I ensure Claude does not repeat incorrect advice about rule logic or other decisions it has previously gotten wrong?
- Can you convert quality scoring dimensions into an interactive graph visualization?
- How is Maverick's custom agent harness different from the Claude Agent SDK (CASDK)? What are we gaining and what are we trading away?
- Is there a way to build the agent harness with significantly less custom surface area while still getting the desired plugin/composition behavior?
- Most documentation was generated by AI — how do we validate that AI-generated audit docs, gap analyses, and architecture docs are actually correct and not just plausible-sounding?
- What happens when a user discusses multiple deals or multiple funds in a single context — how does the memory model handle that?
- How do I build a graph from competitive research data — what's the right schema, and do I need LLM at every step or just for specific parts?
- Is 'no technical detail in specs' a universal industry standard, or does it apply only to Software Requirements Specifications (SRS) — and how does spec-kit handle this?
- HTTP 500s from enrichment endpoints (person, profile_updates, company_employee) due to 'No free account' provider exhaustion — how to handle and monitor?

Cross-person demand:
  Can Claude autonomously poll another Claude Code session's JSONL conversation file every 60-120 seconds and report progress without user input? <-> How can I disable CI tests on GitHub without touching local hooks, and re-enable when needed?
  Can Claude autonomously poll another Claude Code session's JSONL conversation file every 60-120 seconds and report progress without user input? <-> How is Maverick's custom agent harness different from the Claude Agent SDK (CASDK)? What are we gaining and what are we trading away?
  How can I disable CI tests on GitHub without touching local hooks, and re-enable when needed? <-> Can Claude autonomously poll another Claude Code session's JSONL conversation file every 60-120 seconds and report progress without user input?
  How can I disable CI tests on GitHub without touching local hooks, and re-enable when needed? <-> What is the correct OR/AND group logic for rule conditions, especially for equals vs not_equals conditions across single-value fields?
  How can I disable CI tests on GitHub without touching local hooks, and re-enable when needed? <-> Is 'no technical detail in specs' a universal industry standard, or does it apply only to Software Requirements Specifications (SRS) — and how does spec-kit handle this?